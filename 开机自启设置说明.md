# 开机自启设置说明

Proxy429 是**纯托盘 / 菜单栏应用**：启动后没有终端窗口，状态灯在系统托盘（Windows/Linux）或菜单栏（macOS）。要看到图标，它必须在**用户登录会话**里启动。

所以三个平台都用**用户级自启**（登录后启动），**不要**装成系统服务（Windows Service / launchd daemon / systemd service）--系统服务跑在无桌面的独立会话里，托盘图标不会出现。

> 代理运行路径示例：Windows `C:\RUN_Utility\Proxy429\proxy429.exe`；macOS `/Applications/Proxy429.app`；Linux `/opt/proxy429/proxy429`。下面把 `<安装路径>` 换成你实际的位置即可。

---

## Windows

### 方法 A：启动文件夹（推荐，最简单）

1. `Win + R` 打开「运行」，输入 `shell:startup` 回车--会打开当前用户的启动文件夹（`%AppData%\Microsoft\Windows\Start Menu\Programs\Startup`）。
2. 找到你的 `proxy429.exe`，**右键拖进启动文件夹**，松手后选「在此处创建快捷方式」。
3. 完成。下次开机登录后代理自动启动，托盘出现状态灯。

### 方法 B：注册表（适合脚本 / 命令行）

以**当前用户**身份写注册表 `HKCU\...\Run`（不需要管理员权限）。在 PowerShell 或 cmd 里：

```cmd
reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Run" /v Proxy429 /t REG_SZ /d "\"C:\RUN_Utility\Proxy429\proxy429.exe\"" /f
```

> 路径里的引号要保留（`\"...\"`），确保含空格的路径能整体解析。

**取消自启**：

```cmd
reg delete "HKCU\Software\Microsoft\Windows\CurrentVersion\Run" /v Proxy429 /f
```

或直接删启动文件夹里的快捷方式。

---

## macOS

### 方法 A：登录项（推荐，最简单）

1. 把 `Proxy429.app` 拖到 `/Applications/`（推荐位置）。
2. 打开「**系统设置 -> 通用 -> 登录项与扩展**」（macOS 13+；旧版是「系统偏好设置 -> 用户与群组 -> 登录项」）。
3. 点「+」，选 `/Applications/Proxy429.app` 添加。
4. 完成。下次开机登录后菜单栏自动出现状态灯。

### 方法 B：LaunchAgent（适合命令行 / 自动化）

创建 `~/Library/LaunchAgents/com.proxy429.app.plist`：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.proxy429.app</string>
  <key>ProgramArguments</key>
  <array>
    <string>/usr/bin/open</string>
    <string>-a</string>
    <string>/Applications/Proxy429.app</string>
    <string>--args</string>
    <string>-config</string>
    <string>/Users/你的用户名/Library/Application Support/proxy429/config.json</string>
  </array>
  <key>RunAtLoad</key>
  <true/>
  <key>KeepAlive</key>
  <false/>
</dict>
</plist>
```

> 用 `open -a` 启动 `.app` 才能正确进入菜单栏 GUI 会话；直接跑 `Proxy429.app/Contents/MacOS/proxy429` 虽能启动但可能缺 GUI 上下文。
> `--args -config ...` 那两行可选，不需要指定配置就删掉。

加载与验证：

```bash
launchctl load ~/Library/LaunchAgents/com.proxy429.app.plist
launchctl list | grep proxy429      # 有输出 = 已加载
```

**取消自启**：

```bash
launchctl unload ~/Library/LaunchAgents/com.proxy429.app.plist
rm ~/Library/LaunchAgents/com.proxy429.app.plist
```

或在「登录项」里选中 app 点「-」移除。

---

## Linux

桌面环境（GNOME / KDE / XFCE 等）通用：放一个 `.desktop` 文件到 `~/.config/autostart/`。

创建 `~/.config/autostart/proxy429.desktop`：

```ini
[Desktop Entry]
Type=Application
Name=Proxy429
Exec=/opt/proxy429/proxy429
Icon=proxy429
Terminal=false
X-GNOME-Autostart-enabled=true
```

> `Exec` 改成你的实际路径。`Terminal=false` 确保不弹终端窗口。

**取消自启**：删掉 `~/.config/autostart/proxy429.desktop` 即可。

> 无桌面环境（纯命令行服务器，用 systemd 管服务）不在本文档范围--那种场景没有托盘可言，建议直接 `systemctl --user` 跑无头模式，但这不是本程序的设计用途。

---

## 验证自启是否生效

设置完后**重启系统**（或注销再登录），登录后看：

- **Windows**：任务栏右下角托盘出现状态灯图标（灰=空闲）。
- **macOS**：屏幕顶部菜单栏出现状态灯。
- **Linux**：系统托盘区出现状态灯。

没有出现就先检查路径是否正确、配置文件能否正常加载（临时在配置里加 `"log_file": "proxy.log"`，重启后看日志）。

---

## 注意

- 自启的是**当前用户**级，换用户登录不会自动启动。
- 代理监听端口（默认 `127.0.0.1:8080`）只在本机，开机自启不会把端口暴露给内网（除非你另在配置里开了 `allow_remote`）。
- 升级程序：直接覆盖可执行文件（Windows `.exe` / macOS `.app` / Linux 二进制）即可，自启设置不受影响。

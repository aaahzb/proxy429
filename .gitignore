# Build artifacts
*.exe
*.exe~
proxy429
proxy429-*

# macOS directory metadata
.DS_Store

# Log files
*.log

# Local config (contains API keys — never commit)
config.json

# Claude Code local metadata
.claude/


# Local release directory (binaries + config templates, local-only, never committed)
release/
